<div class="row justify-content-center">
  <div class="col col-md-6">
    <h4 align="center"><%= answer_header(@answer) %></h4>

    <%= render 'form', answer: @answer, question: @question %>
  </div>
</div>
